# Gemini API Key
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Neon Database (Vercel Postgres互換)
# Vercelダッシュボード > Storage > Neon Postgres で作成後、接続文字列をコピー
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Vercel Blob Storage
# Vercelダッシュボード > Storage > Blob で作成後、トークンをコピー
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxx

# Note: VITE_ prefix is required for client-side env vars in Vite
# DATABASE_URL and BLOB_READ_WRITE_TOKEN are server-side only (API Routes)
